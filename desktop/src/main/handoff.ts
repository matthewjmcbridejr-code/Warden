import type { WardenRun } from '../shared/types';
export function createHandoff(run: WardenRun): string { const commands = run.events.filter((event) => event.type === 'command.completed').slice(-20).map((event) => `- \`${String(event.payload.command || '')}\` → exit ${String(event.payload.exitCode ?? 'unknown')}`); const approvals = run.approvals.map((approval) => `- ${approval.title}: ${approval.status} — ${approval.detail}`); return [`# Warden handoff: ${run.project}`, '', `- Run: ${run.id}`, `- Provider: ${run.provider}${run.model ? ` / ${run.model}` : ''}`, `- Status: ${run.status}`, `- Working directory: ${run.cwd}`, `- Branch: ${run.evidence.branch || run.context?.branch || 'unknown'}`, `- Codex thread: ${run.threadId || 'none'}`, '', '## Goal', '', run.prompt, '', '## Result', '', run.evidence.finalMessage || run.error || 'No final result recorded.', '', '## Changed files', '', ...(run.evidence.changedFiles.length ? run.evidence.changedFiles.map((file) => `- ${file}`) : ['- None recorded']), '', '## Commands and tests', '', ...(commands.length ? commands : ['- None recorded']), '', '## Approvals', '', ...(approvals.length ? approvals : ['- None']), '', '## Remaining work', '', run.status === 'completed' ? 'Review the diff and proof, then continue from the preserved thread if needed.' : 'Resume this run from Warden AI Desk; inspect the last event and pending approvals first.', ''].join('\n'); }
